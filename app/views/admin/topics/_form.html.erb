<%= form_for([:admin, @admin_topic]) do |f| %>
  <% if admin_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_topic.errors.count, "error") %> prohibited this admin_topic from being saved:</h2>
      <ul>
      <% admin_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.hidden_field :parent_id %>
  </div>
  
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= f.label "Folder" %><br />
    <%= f.collection_select(:parent_id, Topic.order(:title), :id, :title, 
                            {
                              include_blank:true
                            },
                            class: 'form-control'
                            )
    %>
  </div>

  <div class="form-group">
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  </div>
  
  
<% end %>
