<% topics.each do |topic| %>
  <div class="col-md-2">
    <div class="card">
      <div class="card-body">
        <%= link_to topic.title, [:admin, topic], class: 'folder-link' %>
      </div>
    </div>
  </div>
<% end %>